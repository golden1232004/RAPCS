#include <iostream>
#include <algorithm>
#include <math.h>
#include <vector>
using namespace std;
namespace Euclidean {

	double Euclidean_distance(const vector<double> &actual, const vector<double> &training);

}